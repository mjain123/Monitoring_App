{
  "name": "M4",
  "version": "1.0.0",
  "description": "Clone this repo, run npm install.\r Install redis and run on localhost:6379\r A simple web server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.12.3",
    "heartbeats": "^3.1.0",
    "http-proxy": "^1.10.1",
    "request": "^2.55.0",
    "response-time": "^2.3.0",
    "socket.io": "^1.3.5"
  }
}
